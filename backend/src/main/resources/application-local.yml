spring:
  sql:
    init:
      mode: always
      data-locations: classpath:local-data.sql

  docker:
    compose:
      enabled: true
      file: compose.yml
      lifecycle-management: start-and-stop

logging:
  level:
    com.mametosho: DEBUG
    org.mybatis: DEBUG
